apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aether.fullname" . }}-jaeger
  namespace: {{ .Values.global.namespace }}
data:
  tracing.yaml: |
    sampling:
      strategy: probabilistic
      param: 0.999
    propagation:
      - tracecontext
      - baggage

